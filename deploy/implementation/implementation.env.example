# Merge these into the environment file used by the deployment.
IAM_DEPLOYMENT_MODE=implementation
IAM_ENTERPRISE_LOGIN_REQUIRED=false
# No Keycloak service or new named security/audit owners required for testing.
# Keep existing passwords, JWT/audit keys, tenant and application assignments.
