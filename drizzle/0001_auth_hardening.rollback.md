# 0001_auth_hardening rollback

该迁移只增加 `users.token_version` 并把已知默认弱哈希或非 bcrypt 记录标记为强制改密，不删除业务数据。

回滚应用版本时保留新增列最安全，旧版本会忽略它。若必须撤销强制改密标记，应先导出受影响用户 ID 和审计审批号，再对明确批准的用户执行定向 `UPDATE`；不得全表清除。只有确认所有新会话已失效且旧应用已恢复后，才可考虑删除 `token_version`，生产回滚脚本不自动执行该破坏性动作。
