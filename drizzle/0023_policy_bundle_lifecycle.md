# 0023 策略包全生命周期

## 变更

- 新策略包默认从 `draft` 开始，必须经过测试、双人审批、影子、灰度后才能激活。
- 记录评测证据、归档信息和乐观并发版本。
- 新增只追加的 `policy_bundle_transitions`，保存每次状态变化、操作者、理由和证据。

历史 `pending_approval`、`approved`、`active` 等策略包不做伪造性回填；它们仍可运行，但下一次状态变更会产生第一条真实迁移证据。

## 回滚

该迁移涉及审计证据和发布状态，不提供自动破坏性回滚。需要回滚应用版本时保留新增列和迁移历史表；旧应用会忽略这些字段。
