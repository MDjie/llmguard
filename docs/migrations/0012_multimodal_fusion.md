# Migration 0012: multimodal fusion and provenance

Adds an optional accepted TEXT context artifact to each guard job, immutable derivative lineage,
inverse coordinate mappings and structured cross-modal findings. Raw user text remains in the
object-retention boundary instead of the job row.
