# 升级实施状态与合并后待办

需求来源：输出/升级设计/国舜大模型安全网关平台升级需求分析与设计文档V1.0_2026-09-07.docx。

2026-09-07 用户要求暂停继续测试，将 codex/detection-optimization-v2 分支及当前代码修改融合提交到 main 并推送。以下是合并时的实际状态，不代表全部规划或生产验收已经完成。隔离测试服务已停止，数据库与私有测试材料保留在本地；密钥、运行配置和构建目录不提交。

## 已实现范围

- W01—W05：内部 2.0 契约及跨语言黄金向量、mTLS/HMAC 身份、签名快照、统一 Java HTTP/SSE/WSS 与控制台 BFF、结构化动作及复检、幂等、会话提交、持久执行证据、请求资源预留与最终结算。
- W06：复用模型资格约束，新增独立质量报告和性能门槛工具；没有真实独立模型质量验收。
- W07：白皮书风格控制台、应用与凭据接入、运行快照与节点加载状态、请求动作和资源详情；签名发布、灰度、回滚及默认关闭的影子检测任务。
- W08：有界缓存、业务并发和字符预算、冻结负载与 SSE 测量、逐请求附加延迟核算、运行指标与告警；完整 HA 故障驱动和 24 小时资源采样仍待补充。
- W09：0047—0058 扩展迁移、迁移计划/执行/断点续跑、恢复验证、身份预检、Helm 和 Compose 配置、三个容器镜像及离线包签名/完整性校验工具。
- W10—W11：RAG 检索前 ACL 与模型引用、TEXT 文件引用、工具许可消费/真实执行回执/未知结果对账/独立审批补偿、CodeSentinel 作业适配；协议夹具通过不等于外部产品认证。
- W12：分模块说明及部分工程证据已保存；完整 F/T/U/W/IT 追踪矩阵、白皮书 POC 加权评分和最终能力清单仍未完成。

## 已取得的验证结果

以下结果对应各自运行时版本；最后的少量配置与发布刷新修复未重新跑全套，不能统一当作当前提交全量通过。

| 验证 | 最近已通过结果 |
|---|---|
| 全量 Node 单元测试 | 1,245 项 |
| Java 单元测试 | 41 项，Docker 构建内执行 |
| 桌面及移动端 UI | 20 项 |
| 基础代理链路 | 29 项，真实 Java/Node/PostgreSQL 与合成模型 |
| WINDOW / RAG 模型 / TEXT 文件 | 8 / 7 / 12 项 |
| 工具 / CodeSentinel / RAG 检索 | 18 / 7 / 11 项 |
| 审计批次 / 留存 / 发布 | 7 / 6 / 11 项 |
| 请求资源 / 影子比较 / 身份预检 | 7 / 6 / 6 项 |
| 迁移工具 / 中断续跑 / 备份恢复 | 4 / 3 / 4 项；恢复比较 15 张关键表 |
| Helm 原配置 / 可选影子部署 | 6 / 3 项，仅渲染与结构检查 |
| 新增运行指标 | 2 项单测及 11 个数据库指标读数验证 |
| 离线包完整性工具 | 5 项签名、篡改、路径与 SBOM 元数据测试 |
| 类型检查 / 生产构建 | 最近一次通过；三个运行镜像已构建 |

## 合并后优先继续

1. 验证显式刷新 WINDOW 到 FULL_BUFFER：最后发现刷新仍校验旧资格，已限制该检查仅用于非 refresh 操作或回滚；应补数据库回归，确认自动续期/回滚仍拒绝失效资格。
2. 复跑 TEXT 文件 12 项与基础链路 29 项。最后一次文件复跑在测试准备阶段遇到 STREAM_QUALIFICATION_REVOKED，未进入用例；不能算新一轮通过。随后按用户指示停止测试。
3. 验证 Compose 初始化挂载名称：0057/0058 已修复为独立 67/68 路径；检查脚本已修正 postgres 服务名称。
4. 运行契约、类型、lint、单元和必要集成回归；保留既有失败记录，不以旧报告覆盖最后修改。
5. 对实际镜像做启动冒烟、执行离线 pack/verify/load，并生成真实 SBOM。当前离线工具单测通过，但实际整包导出、签名、SBOM 扫描与导入尚未执行；不能宣称离线整包验收通过。迁移运行时和 SQL 已加入 worker 镜像。
6. 补 HA/依赖故障/恢复测试驱动、24 小时采样、W12 全量追踪与 POC 评分、版本能力档案；汇总并更新脱敏证据。
7. 完成指定硬件标准压测、24 小时长稳、独立模型质量、媒体质量和 AgentGuard/CodeSentinel/IAM/SIEM 等实际联调；目前没有指定测试环境，相关结论保持未验。

## 计量与交付边界

请求资源账本记录字符、检测次数、引用、并发占用和已知释放量；未知结果不会按零消耗处理。业务模型真实 Token 和货币成本没有可靠回执，因此明确未计量，不能宣称完整计费。

短时 G1/SSE 工程测试仅验证测量工具与链路，不代表容量达标。生产 WINDOW 要求独立资格，影子执行默认关闭，外部产品和国产软硬件兼容不预填满足。生产部署、镜像发布与实际数据库升级均未在本轮执行。

详细脚本位于 scripts/integration、scripts/acceptance、scripts/release；私有原始证据位于 .artifact-build/upgrade-implementation-20260907，已脱敏的阶段证据位于 acceptance/gateway-v2/evidence/2026-09-07。
