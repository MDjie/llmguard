# P0-1 中文敏感词库接入与全量验证

已完成 ToxiCN 五类词库的清洗、可复现编译、正式 RuleDetector 接入、治理转换器衔接和 ChineseSafe 全量离线 A/B 验证。状态为本地候选策略可运行；尚未发布线上，尚未把词法候选升级为已确认风险。

## 交付

537 条源记录 → 279 个保留候选词、258 条排除、0 条重复；输入与输出各一条规则，共 558 条。原基准策略 81 条规则，候选策略 639 条，其余策略配置相同。

新增编译器：`src/lib/content-safety/toxiccn-lexicon.ts`。

可执行入口：`scripts/content-safety/toxiccn-import.ts`；旧 `import-toxiccn-candidates.mjs` 已修复为兼容入口，不再隐式合并主词库。

清洗配置：`data/content-safety/lexicon/toxiccn-curation.v1.json`，内容版本 toxiccn-curation-v2。

使用说明：`scripts/content-safety/README.toxiccn.md`。

最终候选资产：`.artifact-build/toxiccn-p0-1-20260910/import-final/`，包含 pack、正式 RuleSpec、待审转换结果、候选快照及清洗来源明细。

最终全量证据：`.artifact-build/toxiccn-p0-1-20260910/chinesesafe-final/`，包含逐条 ledger、summary、前后代码与数据指纹。

## 全量结果

同一份 ChineseSafe 20,000 条输入，10,000 条源标签违规、10,000 条源标签正常。调用的是本项目 TypeScript `createEngineForPolicyBundle`，采用历史冻结的 7 节点策略基准；不是 Python llm-guard 双扫描器，也不是在线服务或当前默认全检测器 DAG。两侧均未启用在线语义模型。

| 口径 | 基准 | 词库候选 |
|---|---:|---:|
| 源标签违规样本有任意风险信号 | 367 | 803 |
| 源标签违规样本无风险信号 | 9,633 | 9,197 |
| 信号未命中率（包含未确认状态） | 96.33% | 91.97% |
| 源标签正常样本有风险信号 | 169 | 198 |
| 信号误触发率 | 1.69% | 1.98% |
| 源标签违规样本被 BLOCK | 315 | 315 |
| 源标签正常样本被 BLOCK | 87 | 87 |
| BLOCK 动作漏拦率 | 96.85% | 96.85% |
| BLOCK 动作误拦率 | 0.87% | 0.87% |

新增捕获 436 条原标签违规样本，新增 29 条正常样本候选告警。增益主要来自脏话侮辱 298 条、淫秽色情 102 条，其他类别共 36 条。词库没有补齐隐私、违法指导、身体伤害等缺少直接粗口的表达。

新增候选默认是 SIGNAL。旧版策略产生 WARN，v2 策略进入 REQUIRE_REVIEW；不能把 436 条新增候选说成已减少 436 条实际漏拦。已确认风险数量未提升，BLOCK 数量也未变化。confirmed 子集会排除无确认且未知的样本，分母随候选数量改变，不能直接比较其 FNR。

全部 20,000 条完成，执行错误 0，网络连接尝试 0，前后代码/数据指纹一致。执行版本代码哈希：`80beddec240186bbc1bde63aaf5021377319f716c56a672f6c0ac2121731b294`。

## 误触发修复与限制

第一版 285 个词引入 42 条正常样本候选告警。排除“姆的”“口区”“倭寇”“鬼子”“牲口”“阿三”六个字面或姓名/历史歧义词后，最终告警降为 29 条，新增原标签违规捕获仍为 436 条。

该迭代使用了同一开发数据集，属于开发复测；不能作为全新独立测试集上的泛化收益。仍保留有召回贡献但存在语境歧义的短词，残留告警需由语义复核处理；未建立整句白名单，也未静默压低阈值。

## 验证与后续集成

本次相关 27 项测试通过，严格 TypeScript 检查通过。覆盖全部 279 个词的双向检测、正常身份/医疗/历史地名、局部引用与否定、同消息恶意片段、零宽字符、来源哈希、稳定 ID、语义候选角色、硬拦优先级及治理转换器消费。

来源锁仍标为 PENDING_REVIEW/candidate_import_only，未锁定原词库上游提交。正式发布需许可核验、词条与语境审查、语义复核和现有发布评估、审批及签名流程。没有假装人工审批或绕过线上门禁。

P0-2/P0-3/P0-4 后续工作另记统一交付报告；本报告反映以上已完成的词库版本。模型微调和真实已确认风险能力必须单独给出证据。
