# P3 会话风险、多模态与资源韧性执行记录

执行时间：2026-09-05（Asia/Shanghai）
执行分支：`codex/security-hardening-v1`
阶段结论：内部代码、迁移、自动化测试、构建与本地容器部署通过；生产视觉/ASR/音频分类/TTS 模型资产仍为外部验收阻塞。

## 实施范围

- 会话风险状态机：`NORMAL / WATCH / ESCALATED / LOCKED`、意图节点、状态迁移、时间与良性轮次衰减、显式会话结束、租户与应用隔离。
- 渐进式攻击识别：边界探测、片段请求、敏感目标、执行请求四阶段链路，命中后逐级提高检测、并发成本与响应动作。
- 资源控制：请求/会话/租户等多作用域配额、媒体时长/帧数/解码分支/Judge/解压量计价、公平队列、租约恢复、取消传播、稳定错误码。
- 图像与文档：多视图 OCR、页/段/行/词层级、二维码/条码、视觉标签、像素/解码字节/解压比限制。
- 音视频：多视图 ASR、说话人及声道证据、字幕提取、帧 OCR、二维码、视觉风险、摘要采样与风险触发扩展采样。
- 融合与韧性：逐模态检测、跨模态组合检测、音频/字幕冲突、超时/有限重试/熔断/舱壁、必需组件失败关闭、`traceId + evidenceRef` 证据绑定。
- 作业取消：客户端断开、任务取消、子进程终止和终态竞争保护，取消后不会被完成或失败状态覆盖。
- 部署隔离：分析器只读根文件系统、4 CPU、8 GiB 内存、256 PID、文件描述符限制及临时目录限制；Helm 增加临时存储配额。

## 自动化验证结果

| 门禁 | 结果 |
|---|---:|
| P3 核心测试 | 16 文件 / 52 测试通过 |
| 文档与内容标识回归 | 5 文件 / 18 测试通过 |
| 全量单元回归 | 142 文件 / 655 测试通过 |
| TypeScript | 通过 |
| 生产 lint | 通过 |
| 媒体分析器打包 | 通过 |
| Next.js 生产构建 | 通过，61 个静态页面生成完成 |
| Compose 配置校验 | 通过 |
| 部署资产测试 | 2 文件 / 12 测试通过 |
| Helm lint | 未执行：本机未安装 `helm`；模板资产测试通过 |

覆盖的关键用例包括：OCR/二维码注入、图文协同攻击、字幕与音频冲突、时间线递进诱导、超大图像、解压炸弹、超长音视频、依赖超时/熔断/舱壁、请求与作业取消、并发预算、租约恢复及必需分析组件失败关闭。

## 数据库验证

- 在一次性 `guardllm_integration_p3_*` 隔离库从零执行 41 个迁移，租户隔离、数据血缘、审计防篡改和策略治理检查全部通过。
- 0039 新增的 10 个列和 4 个约束全部被自动核验。
- 隔离测试库在验证后已删除。
- 0039 已在现有 `guardllm-r0` 数据库内以事务方式应用，现场核验返回 10 个目标列。

## 本地部署验证

- 镜像：`guardllm-r0-app:latest`、`guardllm-r0-worker:latest`、`guardllm-r0-media-analyzer:latest` 构建成功。
- 运行状态：应用、PostgreSQL、媒体分析器及 10 个工作器全部运行；重启次数均为 0。
- 应用：`/api/health/live` 返回正常；`/api/health/policy` 返回 ready，已加载签名策略包 generation 3。
- 分析器：live 返回 200；`zbarimg 0.23.93`、Tesseract 5.5.2、FFmpeg 8.1.2 可用。
- 严重日志扫描：未发现 fatal、panic、未处理异常、连接拒绝、权限错误或 OOM。

## 外部阻塞与判定

媒体分析器 readiness 当前返回 503，原因是本地环境没有配置生产级视觉分类、ASR、音频异常分类和 TTS 可执行模型。服务仍保持 live，二维码、OCR、FFmpeg、资源限制、任务调度和失败关闭逻辑均已部署；缺失模型时分析任务会产生结构化失败证据并按必需组件失败关闭，不会伪造检测成功。

解除阻塞需要由模型供应方提供并验收以下只读挂载资产：

- `ANALYZER_VISUAL_COMMAND`
- `ANALYZER_ASR_COMMAND`
- `ANALYZER_AUDIO_CLASSIFIER_COMMAND`
- `ANALYZER_TTS_COMMAND`

生产词库来源审批和真实生产模型权重不属于代码仓库可自行生成的资产，继续保持外部验收阻塞。
