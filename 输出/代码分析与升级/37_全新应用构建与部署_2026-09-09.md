# 全新应用构建与部署记录（2026-09-09）

应用已构建、切换并完成实际访问验证。访问地址：http://127.0.0.1:58082 。

## 交付范围

- 基础源码：main 已推送提交 e1852e366b57bea499a88f98de352143522ce88e。
- 试验部署发现策略健康接口返回 detectionCapabilities，但严格响应契约未声明该字段，导致 HTTP 500。本轮补齐明确字段校验并增加 3 项回归测试；这两项源码变更保留在工作区，尚未提交或推送。
- 补丁文件：src/app/api/health/policy/route.ts、tests/policy-bundle/health-route.test.ts。实际路由文件 SHA256：c760baec7804a3a05ebe9925aa4c2d867e01da19890448e644719a40fc7e6606。
- 网页、Worker、媒体解析器均从本轮冻结源码重新构建，并带有基础提交及补丁哈希标签。759 个源码和依赖文件与最终 Worker 镜像逐一比对一致。
- 已更新 1 个网页应用、1 个解析器、14 个 Worker；17 个项目容器均运行且重启计数为 0。9 类已接入心跳的 Worker 均持续更新 running 状态。

## E 盘 Docker

- 当前真实数据目录为 E:\DockerData\wsl，包括镜像、容器和数据库所处的 docker_data.vhdx。
- C:\Users\ROG\AppData\Local\Docker\wsl 是指向上述 E 盘目录的 Junction。两条数据盘路径的文件 ID 相同，当前并非两份独立数据。
- Docker Desktop 程序本体仍安装于 C:\Program Files\Docker\Docker。本轮没有删除 C 盘程序或数据。后续清理 C 盘时应先完成程序迁移，并识别该目录链接，避免误删实际位于 E 盘的数据。
- Docker 启动故障已恢复：保留失效 IPC 文件后恢复 Docker/WSL 管理服务；未恢复出厂设置，未删除容器、镜像或数据卷。

## 构建与数据库验证

- Windows 独立 Next.js 生产构建及媒体解析器构建通过。
- Docker Hub 基础镜像下载不可达，因此使用本地已有的 Node 24.20.0 镜像作为构建基础。所有旧应用源码在构建阶段移除；通过 pnpm 11.19.0 和最新 frozen lockfile 安装新增的 @noble/hashes、saxes、xmlchars，随后完整重新编译新代码。
- Next.js 16.3.3 生产编译和 TypeScript 检查通过；健康接口相关 9 项单元测试通过；两个修改文件 ESLint 零警告。
- 数据库使用一致性快照备份，恢复到独立 guardllm_build_e1852e3_20260909 数据库，105 张表行数完全匹配。
- 0065–0072 共 8 项迁移先在恢复库通过，再根据同一 SQL 哈希计划应用到 guardllm。切换前额外完成一次备份。
- 原数据库容器 ID 和全部 42 个挂载均保持一致，原件独立授权触发器已启用。
- 原地址 /login、/api/health/live、/api/health/db、/api/health/policy 均返回 200；未认证的 /api/stats 返回 401。真实浏览器登录页加载成功，无页面脚本错误和静态资源失败。
- 临时 58083 试验实例已停止；试验数据库与备份保留。

## 尚未改变的验收状态

当前保留原有已签名策略，未重新签名或擅自增加检测器。策略报告仍为 LEGACY_UNDECLARED、runtimeCompatible=false，旧策略缺少 9 个新版必要检测器。应用运行成功不表示这些新增能力已经启用，后续需通过策略发布流程升级。

真实 ASR／视觉／音频分类配置、独立双人标签及质量验收仍未准备完成；qualityQualified=null、mediaRuntimeVerified=false。本轮没有宣称通过生产质量验收，也没有启用额外的自动清理 Worker。

## 本地证据与恢复材料

所有构建日志、冻结源码、补丁、镜像清单、迁移计划、HTTP/浏览器验证、数据库备份、现有运行配置和回滚配置均位于：

E:\大模型安全\大模型护栏\.artifact-build\new-app-e1852e3-20260909

- deployment-verification.json：最终运行与结构验证。
- image-verification.json、source-patch.json：镜像与源码标识。
- before.private.dump、cutover.private.dump：数据库备份，仅供本地维护。
- compose.private.json、rollback-compose.private.json：新版本及原镜像配置，包含私有运行配置，不应发布。
- deployed-login.png：实际运行页面截图。

回滚材料保留原镜像与密钥映射；本轮新增数据库结构未进行逆向删除。
