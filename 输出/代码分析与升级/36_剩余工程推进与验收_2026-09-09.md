# 剩余工程推进与验收

日期：2026-09-09；基线：c31de18570c2437f45ab0ce916644aeca73059b4（main）。本报告记录 I–L 轮，本轮全部研发和质量验收仍未关闭；用户已明确真实模型配置和独立双人标签尚未准备，继续延期。

已完成 R04/R09 的独立原件与 PDF 页预览、正反证归档回放，补齐 R01/R07 的明确覆盖缺口，以及 R08 的实际签名过期和数据库恢复演练。R09 在已声明格式与预算内更新为 ENGINEERING_VERIFIED。

## 实际完成

| 范围 | 实现与验证 | 边界 |
|---|---|---|
| 原件和 PDF | MEDIA_ORIGINAL 独立双人审批，按来源/页码/任务/所有者/摘要绑定；数据库迁移 0072 同步约束；有效原件授权也保护证据快照 | 文本审批不会获得原件权限；失败不消费授权；并发只成功一次 |
| 大文件与定位 | 真实 5 MiB PDF 和 5 MiB WAV 完整读取；PDF 服务端单页栅格化；图片框、PDF 页和音频时间点按已验证坐标回放 | 原件媒体 16 MiB，PDF 输入 256 MiB/2000 页、输出 8 MiB/1600 像素；原生模型输入仍为 1 MiB |
| 正反证 | 支持证据、反证、模型版本和判定角色进入加密快照；绑定来源内容版本和精确位置；保留其他语义窗口的证据 | 其他窗口只追加可审查证据，不单独取得完整语义覆盖资格；保留既有风险处置 |
| 格式边界 | 多视频流显式拒绝静默漏轨；旧 Office/RTF 隐藏内容未枚举时保留 INCOMPLETE；能力页列出 profile 限制 | 不把解码器可用当作语义安全；HEIF 序列和 OFD 等仍需适配/鉴定 |
| 真实清理时效 | 实际等待 60 秒签名及 65 秒排空；提前不能删，过期链接不能重建清理对象，确认为空后释放配额 | 仅本轮新建合成对象；不是长期观察或生产清理启用 |
| 数据库恢复 | pg_dump 恢复至新测试库，8 类关键表摘要一致；已消费授权、原件约束、密文及原件摘要回放通过 | 对象桶和密钥仍保留，没有模拟它们丢失 |

## 最终验收

- **L 轮完整冷启动：BLOCKED，42 个阶段、0 执行失败。** 阻塞项为媒体语义与发布资格，未修改门槛。
- 完整单元与契约测试：**1789 / 1789 PASS**，0 失败、0 跳过。
- 原件/PDF/正反证数据库及对象存储检查 13 项、实际浏览器交互 6 项、真实格式边界 10 项通过。
- 真实签名排空 6 项、数据库备份恢复 5 项、派生聊天 27 项、清理恢复 6 项、标准资产/清理 13 项、派生 RAG 10 项通过。
- 23 个页面巡检、主要业务流程、权限、分层证据、180 天查询边界、Worker/解析器实际停机恢复通过。不是所有路由与模型组合的穷举覆盖。
- 49 个媒体/混合用例：**6 PASS、43 BLOCKED、0 FAIL**。阻塞案例维持拦截或人工复核，没有被计为安全通过。
- TypeScript、修改范围 ESLint（0 错误/警告）、解析器及 Next.js 生产构建、公共合同兼容检查通过。L 轮源码指纹 cd3be96f912eac02c4ee2d5bc12f133a9500450c4384d0ff38e4be9c2b30e745，报告生成时全部冻结文件仍匹配。

机器证据：[36_剩余工程证据_2026-09-09/verification.json](36_剩余工程证据_2026-09-09/verification.json)。原始失败、日志、会话、备份和密钥仅留在忽略目录 .artifact-build；没有纳入提交。

## 失败记录与避坑

1. 新增资源类型必须同时迁移数据库 CHECK 和来源约束；不能只改前端枚举。
2. pnpm 严格依赖布局下不能直接引用未声明的 esbuild；验收脚本改用已有 tsup。
3. 已归档快照的到期时间不可修改。临界测试改为创建合法的临近到期任务，没有删除不可变触发器。
4. 前轮 Docker 退出后端口曾短暂未释放。先确认占用与所有权再重试，没有结束其他服务。
5. 一次性授权必须在浏览器验收前重新签发，避免前序阶段耗时造成自然过期。直接认领任务、真实 TTL 和恢复演练安排在后台 Worker 前。
6. J、K 的失败与 I 的初次失败均保留。最终结果来自 L 单次完整运行，不用多轮成功片段替代。

## 剩余安排

| 顺序 | 任务 | 必要条件与下一步 |
|---|---|---|
| 1 | R01 / R07 | OFD 受控适配器、旧 Office/WPS 隐藏对象、AMR、HEIF 序列及多视频流的实际样本和正常/风险/损坏/超限鉴定；当前显式保留覆盖缺口。 |
| 2 | R04 / R05 / R06 / R10 | 按用户已确认延期的真实 ASR/视觉/音频分类/原生联合配置，验收对应格式与模型路由、文档/混合聊天及两类媒体 RAG。原件预览仅在明确格式和预算内开放。 |
| 3 | R11 / FX12 | 双人审定的开发/独立标签、真实消融、阈值校准和冻结质量评测；不得以合成工程样本替代。 |
| 4 | R08 | 对象桶与密钥的灾难恢复、长期保留观察，以及明确部署范围后的清理 Worker 启用；当前只完成专用数据库恢复和真实签名排空。 |
| 5 | R12 / FX03 / FX05 / FX09 / FX10 / FX11 | 合格策略完整发布生命周期、G0–G5、影子/灰度及业务观察。当前冷启动工程回归不授予生产资格。 |

R10、R11、FX12 所需配置与标签按用户决定延期，不重复索取、不代造标签或批准记录。稀有 profile 适配、完整语义质量、对象存储/密钥灾难恢复、长期观察及生产影子/灰度仍未完成，不能把本轮工程通过写成全部任务完成。

L 轮专用数据库：guardllm_integration_full_1788920987180；恢复数据库：guardllm_integration_full_1788921182367。本轮登记的应用、Worker、解析器已停止；保留专用测试数据库、对象桶和失败证据。未修改生产 58082，未启用默认删除 Worker。

上一轮自动审批曾拒绝默认启用删除型 Worker，理由是共享/生产范围与不可逆删除风险未明确。本轮未重试该部署动作，只进行明确隔离的合成对象验收。

已有部署升级时，须先执行新增迁移 drizzle/0072_original_media_access.sql，再启动新版应用和解析器。本轮只在隔离测试库执行迁移，没有更新生产数据库。
